version: '3.8'
services:
  app:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - ./:/app:delegated
    environment:
      - PYTHONUNBUFFERED=1
    command: streamlit run main.py --server.port=8501 --server.address=0.0.0.0
